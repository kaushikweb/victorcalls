import { ComponentFactoryResolver, ViewContainerRef } from '@angular/core';
import { IModalDialogOptions } from './modal-dialog.interface';
import { ModalDialogInstanceService } from './modal-dialog-instance.service';
export declare class ModalDialogService {
    private componentFactoryResolver;
    private modalDialogInstanceService;
    /**
     * CTOR
     * @param componentFactoryResolver
     * @param modalDialogInstanceService
     */
    constructor(componentFactoryResolver: ComponentFactoryResolver, modalDialogInstanceService: ModalDialogInstanceService);
    /**
     * Open dialog in given target element with given options
     * @param  {ViewContainerRef} target
     * @param  {IModalDialogOptions} options?
     */
    openDialog(target: ViewContainerRef, options?: Partial<IModalDialogOptions<any>>): void;
}
